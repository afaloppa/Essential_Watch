# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Repository status

This repository is in an initial/empty state. It currently contains only a `LICENSE` (MIT) and a placeholder `README.md` ("# Essential_watch"). There is no source code, build system, test suite, dependency manifest, or CI configuration yet.

When code is first added, regenerate this file via `/init` and document:
- Build, lint, test, and run commands (including how to run a single test)
- The big-picture architecture once it spans multiple files/modules
- Any project-specific conventions that aren't obvious from reading the code
